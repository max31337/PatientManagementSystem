@model PatientManagementSystem.ViewModels.PaymentViewModel

@{
    ViewData["Title"] = "Update Patient";
    ViewData["HideFooter"] = true;
}

<div class="container mx-auto px-4 py-6">
    <form asp-action="Update" method="post" class="bg-white p-6 rounded-lg shadow-lg">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="PatientId" />

        <h2 class="text-3xl font-bold text-stromboli mb-6">Update Payment</h2>

        <!-- Display the patient info -->
        <div class="form-group mb-4">
            <label for="PatientInfo" class="block text-gray-700 font-semibold">Selected Patient (ID and Name):</label>
            <input type="text" id="PatientInfo" class="form-control mt-2 p-2 border border-gray-300 rounded-md w-full" readonly
                   value="ID: @Model.PatientId - @Model.FullName" />
        </div>

        <!-- Services Availed -->
        <div class="form-group mt-3">
            <label for="ServicesAvailed">Services Availed:</label>
            <input asp-for="ServicesAvailed" class="form-control" id="ServicesAvailed" required />
            <span asp-validation-for="ServicesAvailed" class="text-danger"></span>
        </div>

        <!-- Amount Paid -->
        <div class="form-group mt-3">
            <label for="AmountPaid">Amount Paid:</label>
            <input asp-for="AmountPaid" class="form-control" id="AmountPaid" type="number" step="0.01" required />
            <span asp-validation-for="AmountPaid" class="text-danger"></span>
        </div>

        <!-- Invoice Number -->
        <div class="form-group mt-3">
            <label for="InvoiceNumber">Invoice Number:</label>
            <input asp-for="InvoiceNumber" class="form-control" id="InvoiceNumber" maxlength="50" required />
            <span asp-validation-for="InvoiceNumber" class="text-danger"></span>
        </div>

        <!-- Payment Date -->
        <div class="form-group mt-3">
            <label for="PaymentDate">Payment Date:</label>
            <input asp-for="PaymentDate" class="form-control" type="date" required />
            <span asp-validation-for="PaymentDate" class="text-danger"></span>
        </div>

        <!-- Payment Status (Dropdown) -->
        <div class="form-group">
            <label for="IsPaid">Payment Status:</label>
            <select asp-for="IsPaid" asp-items="ViewBag.PaymentStatus" class="form-control" required>
                <option value="">-- Select Payment Status --</option>
            </select>
        </div>

        <div class="flex justify-between mt-6">
            <button type="submit" class="bg-stromboli text-white font-semibold py-2 px-6 rounded-md hover:bg-stromboli-dark transition duration-300">
                Update Payment
            </button>
            <a asp-controller="Payment" asp-action="Index" class="bg-gray-300 text-gray-700 font-semibold py-2 px-6 rounded-md hover:bg-gray-400 transition duration-300">
                Cancel
            </a>
        </div>
    </form>
</div>


