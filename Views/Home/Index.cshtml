@{
    ViewData["Title"] = "Home Page";
    ViewData["HideSidebar"] = true;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="flex justify-center space-x-4 mt-4">
    <a href="@Url.Action("Login", "Account")" class="bg-appleGreen text-gray-700 font-bold py-2 px-4 rounded hover:bg-stromboli transition duration-300">
        Login
    </a>
    <a href="@Url.Action("Register", "Account")" class="bg-appleGreen text-gray-700 font-bold py-2 px-4 rounded hover:bg-stromboli transition duration-300">
        Register
    </a>
</div>
